"""Tenant lifecycle operations (provisioning, updates, suspension).

Concrete implementations will integrate with the metadata layer and
security modules to orchestrate tenant onboarding and maintenance flows.
"""

